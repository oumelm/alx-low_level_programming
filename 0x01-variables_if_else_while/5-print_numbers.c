#include <stdio.h>

/**
 * main - prints the alphabet in lowrcase
 * followed by a new line, except q and e
 * Return: Always 0
 */
int main(void)
{
	int num;

	for (num = 0; num < 10; num++)
		printf("%d", num);

	printf("\n");

	return (0);
}
